name: Static repository checks
run-name: Static checks — ${{ github.ref_name }} @ ${{ github.sha }}

# Canonical portable source-integrity gate plus a content-pinned authoritative
# workflow validator. These complement, but never replace, Excel compilation,
# regression, numerical-accuracy, UI-state, packaging, or release certification.

on:
  pull_request:
    branches:
      - main
      - "release/**"
  push:
    branches:
      - main
      - "release/**"
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: static-checks-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  repository-integrity:
    name: Repository integrity
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      PYTHONUTF8: "1"
      PYTHONDONTWRITEBYTECODE: "1"
      ACTIONLINT_VERSION: "1.7.12"
      ACTIONLINT_LINUX_AMD64_SHA256: "8aca8db96f1b94770f1b0d72b6dddcb1ebb8123cb3712530b08cc387b349a3d8"

    steps:
      - name: Check out the exact SHA under review
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install the pinned authoritative workflow validator
        id: validator-install
        continue-on-error: true
        run: |
          validator_dir="${RUNNER_TEMP}/actionlint-${ACTIONLINT_VERSION}"
          archive="${validator_dir}/actionlint.tar.gz"
          validator="${validator_dir}/actionlint"
          mkdir -p "$validator_dir" test-results
          curl \
            --fail \
            --location \
            --proto '=https' \
            --retry 3 \
            --retry-all-errors \
            --show-error \
            --silent \
            --tlsv1.2 \
            --output "$archive" \
            "https://github.com/rhysd/actionlint/releases/download/v${ACTIONLINT_VERSION}/actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz"
          printf '%s  %s\n' "$ACTIONLINT_LINUX_AMD64_SHA256" "$archive" \
            | sha256sum --check --strict
          tar --no-same-owner -xzf "$archive" -C "$validator_dir" actionlint
          "$validator" -version | tee test-results/actionlint-version.txt
          printf 'path=%s\n' "$validator" >> "$GITHUB_OUTPUT"

      - name: Validate workflows and authoritative fixtures
        id: authoritative-workflows
        continue-on-error: true
        env:
          ACTIONLINT_PATH: ${{ steps.validator-install.outputs.path }}
        run: >-
          python3 tools/test_workflow_validation.py
          --root .
          --actionlint "$ACTIONLINT_PATH"
          --summary test-results/workflow-validation.md

      - name: Exercise release-integrity candidates and failures
        id: release-self-test
        continue-on-error: true
        run: >-
          python3 tools/check_release.py
          --root .
          --self-test
          --summary test-results/release-self-test.md

      - name: Exercise all repository-initialization profiles
        id: initializer-self-test
        continue-on-error: true
        run: python3 tools/initialize_repository.py --root . --self-test

      - name: Exercise positive and degraded checker paths
        id: self-test
        continue-on-error: true
        run: python3 tools/check_repo.py --root . --self-test

      - name: Validate repository integrity
        id: gate
        continue-on-error: true
        run: >-
          python3 tools/check_repo.py
          --root .
          --output test-results/static-checks.json
          --summary test-results/static-checks.md

      - name: Publish readable job summary
        id: summary
        if: always()
        continue-on-error: true
        run: |
          {
            if [[ -f test-results/static-checks.md ]]; then
              cat test-results/static-checks.md
            else
              echo "## Static repository checks"
              echo
              echo "The checker did not produce its Markdown report."
              exit 1
            fi
            echo
            if [[ -f test-results/workflow-validation.md ]]; then
              cat test-results/workflow-validation.md
            else
              echo "## Authoritative workflow validation"
              echo
              echo "The authoritative validator did not produce its report."
              exit 1
            fi
            echo
            if [[ -f test-results/release-self-test.md ]]; then
              cat test-results/release-self-test.md
            else
              echo "## Release-integrity self-test"
              echo
              echo "The release checker did not produce its self-test report."
              exit 1
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload static-check evidence
        id: artifact
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: static-checks-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            test-results/static-checks.json
            test-results/static-checks.md
            test-results/actionlint-version.txt
            test-results/workflow-validation.md
            test-results/release-self-test.md
          if-no-files-found: error
          retention-days: 30
          compression-level: 9

      - name: Enforce self-test and repository results
        if: always()
        env:
          INITIALIZER_SELF_TEST_OUTCOME: ${{ steps.initializer-self-test.outcome }}
          VALIDATOR_INSTALL_OUTCOME: ${{ steps.validator-install.outcome }}
          AUTHORITATIVE_WORKFLOWS_OUTCOME: ${{ steps.authoritative-workflows.outcome }}
          RELEASE_SELF_TEST_OUTCOME: ${{ steps.release-self-test.outcome }}
          SELF_TEST_OUTCOME: ${{ steps.self-test.outcome }}
          GATE_OUTCOME: ${{ steps.gate.outcome }}
          SUMMARY_OUTCOME: ${{ steps.summary.outcome }}
          ARTIFACT_OUTCOME: ${{ steps.artifact.outcome }}
        run: |
          failed=0
          if [[ "$VALIDATOR_INSTALL_OUTCOME" != "success" ]]; then
            echo "::error::Authoritative-validator installation outcome: $VALIDATOR_INSTALL_OUTCOME"
            failed=1
          fi
          if [[ "$AUTHORITATIVE_WORKFLOWS_OUTCOME" != "success" ]]; then
            echo "::error::Authoritative workflow-validation outcome: $AUTHORITATIVE_WORKFLOWS_OUTCOME"
            failed=1
          fi
          if [[ "$RELEASE_SELF_TEST_OUTCOME" != "success" ]]; then
            echo "::error::Release-integrity self-test outcome: $RELEASE_SELF_TEST_OUTCOME"
            failed=1
          fi
          if [[ "$INITIALIZER_SELF_TEST_OUTCOME" != "success" ]]; then
            echo "::error::Initializer self-test outcome: $INITIALIZER_SELF_TEST_OUTCOME"
            failed=1
          fi
          if [[ "$SELF_TEST_OUTCOME" != "success" ]]; then
            echo "::error::Checker self-test outcome: $SELF_TEST_OUTCOME"
            failed=1
          fi
          if [[ "$GATE_OUTCOME" != "success" ]]; then
            echo "::error::Repository gate outcome: $GATE_OUTCOME"
            failed=1
          fi
          if [[ "$SUMMARY_OUTCOME" != "success" ]]; then
            echo "::error::Job-summary outcome: $SUMMARY_OUTCOME"
            failed=1
          fi
          if [[ "$ARTIFACT_OUTCOME" != "success" ]]; then
            echo "::error::Evidence-upload outcome: $ARTIFACT_OUTCOME"
            failed=1
          fi
          exit "$failed"
